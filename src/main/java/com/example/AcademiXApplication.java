package com.example;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class AcademiXApplication {
	//todo The scope of this project is it an online courses platform where people can buy course here and can access there course on there login id's and also we can make user entity which is like an admin here


	public static void main(String[] args) {
		SpringApplication.run(AcademiXApplication.class, args);
	}

}
